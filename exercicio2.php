<?php
	//get inputs
	
	//iterate over N (for both companies) to create an array of origin and destination
	
	//order both arrays
	
	//fill variable $return = 'YES'
	
	//compare origin and destination from both arrays looking if it has a direct connection or has some connections from point A to B
	//maybe using a recursive function to do so
	
	//if just one comparison don't match a connection from point A to B $return = 'NO'
	//return echo $return;
?>